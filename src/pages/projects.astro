---
import Base from "../layouts/Base.astro";
import { detectLocale, t } from "../lib/i18n";
import projectsData from "../data/projects.json";

const locale = detectLocale(Astro.url.pathname);
const totalCount = projectsData.categories.reduce((s, c) => s + c.items.length, 0);

// Search fallback so every project links somewhere meaningful.
const projectUrl = (p) =>
  p.url ?? `https://www.osti.gov/search/semantic:${encodeURIComponent(p.name + " Lu Zhang Argonne")}`;

const collaborations = [
  ["MIT", "Prof. Fikile Brushett — anolyte molecule design", "since 2014"],
  ["PNNL", "Flow battery testing & cell engineering", "since 2015"],
  ["UIUC", "Electrolyte design & molecular modeling", "since 2015"],
  ["University of Michigan", "ROMs stability studies", "since 2016"],
  ["Daimler", "Industrial overcharge-protection translation", "2020"],
];

const teamGrowth = [
  ["2013", "1 postdoc"],
  ["2014", "1 postdoc + 1 student"],
  ["2015", "1 postdoc + visiting student · led JCESR low-potential Sprint (3 universities + 2 labs)"],
  ["2016 – 2017", "2 postdocs + 2 students · led electrolyte-optimization Sprint (UIUC, PNNL, ANL)"],
  ["2018", "3 postdocs + 1 student · JCESR 2.0 sub-thrust lead"],
  ["2020 – 2023", "3 postdocs + 1 student · JCESR Y3 milestone lead"],
];
---
<Base title={(locale === "zh" ? "项目 — " : "Funded Projects — ") + "Lu Zhang"}>
  <div class="container" style="padding-top: 3rem;">
    <span class="eyebrow">{t("page.projects.eyebrow", locale)}</span>
    <h1>{t("page.projects.title", locale)}</h1>
    <p class="lead">{t("page.projects.lead", locale)}</p>

    {projectsData.categories.map((cat) => (
      <section style="margin-top: 3rem;">
        <div class="section-head">
          <h2 style="margin: 0;">{cat.title}</h2>
          <span class="more">{cat.items.length}</span>
        </div>
        <p style="color: var(--c-muted); font-size: 0.95rem; margin-top: -0.5rem;">{cat.subtitle}</p>
        {cat.items.map((p) => (
          <div class="entry">
            <div class="title"><a href={projectUrl(p)} target="_blank" rel="noopener">{p.name}</a></div>
            <div class="meta">
              <strong style="color: var(--c-fg);">{p.funder}</strong> · {p.years} · {p.role}
            </div>
            {p.highlights && p.highlights.length > 0 && (
              <ul style="margin: 0.5rem 0 0; padding-left: 1.2rem; color: var(--c-fg); font-size: 0.93rem; line-height: 1.55;">
                {p.highlights.map((h) => <li>{h}</li>)}
              </ul>
            )}
          </div>
        ))}
      </section>
    ))}

    <section style="margin-top: 3.5rem;">
      <div class="section-head"><h2 style="margin:0;">{t("page.projects.capability", locale)}</h2></div>
      <p style="color: var(--c-muted); font-size: 0.95rem; margin-top: -0.5rem;">
        Cross-cutting expertise developed across the projects above.
      </p>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 0.75rem; margin-top: 1rem;">
        {projectsData.capability_themes.map((t) => (
          <div style="background: var(--c-card); border: 1px solid var(--c-border); border-radius: var(--radius-sm); padding: 0.9rem 1rem; font-size: 0.92rem;">
            {t}
          </div>
        ))}
      </div>
    </section>

    <section style="margin-top: 3.5rem;">
      <div class="section-head"><h2 style="margin:0;">{t("page.projects.team", locale)}</h2></div>
      <table>
        <thead><tr><th style="width: 180px;">Year</th><th>Group / leadership</th></tr></thead>
        <tbody>
          {teamGrowth.map(([y, g]) => (
            <tr><td style="white-space:nowrap; font-weight:500;">{y}</td><td>{g}</td></tr>
          ))}
        </tbody>
      </table>
    </section>

    <section style="margin-top: 3.5rem;">
      <div class="section-head"><h2 style="margin:0;">{t("page.projects.collab", locale)}</h2></div>
      <table>
        <tbody>
          {collaborations.map(([inst, desc, when]) => (
            <tr>
              <td style="white-space:nowrap; font-weight:600; color: var(--c-fg);">{inst}</td>
              <td>{desc}</td>
              <td style="white-space:nowrap; color: var(--c-muted);">{when}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </section>

  </div>
</Base>
