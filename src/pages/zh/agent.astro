---
import Base from "../../layouts/Base.astro";
import ChemGraphDemo from "../../components/ChemGraphDemo.astro";
import { u } from "../../lib/url";
---

<Base
  title="实时 Agent Demo — ChemGraph 科学闭环 — 张鲁 Lu Zhang"
  description="一个实时的 agentic 化学 demo:真实的 ChemGraph agent(Argonne LCF,LangGraph + ASE)识别分子、跑真实的单点能仿真,并对照工作流护栏审计自己的结果 —— 张鲁在做的 loop engineering 的一个可运行示例。"
>
  <div class="container agent-page">
    <ChemGraphDemo locale="zh" />

    <aside class="cm-more">
      <span class="cm-more-eyebrow">更多在线演示</span>
      <a href={u("/zh/agent/condition-monitoring/")} class="cm-more-card">
        <strong>设备状态监测管道智能体 →</strong>
        <span>
          确定性管道智能体 + 人工决策把关：在 NASA 公开数据上跑 10
          阶段剩余寿命分析，交回带证据和引用的决策卡——默认路径无 LLM。
        </span>
      </a>
      <a href={u("/zh/agent/preventive-health-model-lab/")} class="cm-more-card" style="margin-top:12px">
        <strong>预防健康模型实验室 →</strong>
        <span>
          8GB 单卡上的受控 QLoRA 微调研究：医疗预训练（MedGemma）相比同底座的
          Gemma 3，对预防健康推理是否更强？合成数据、安全导向评估、诚实结果。
        </span>
      </a>
      <a href={u("/zh/agent/redox-rfb-predictor/")} class="cm-more-card" style="margin-top:12px">
        <strong>液流电池氧化还原电位预测器 →</strong>
        <span>
          输入 SMILES，运行一个公开、快速的水系有机分子氧化还原电位预测器。输出在
          RedDB DFT 参考尺度上，适合分子筛选；页面明确给出适用域与实验限制。
        </span>
      </a>
    </aside>
  </div>
</Base>

<style>
  .agent-page { padding-top: 30px; padding-bottom: 64px; }
  .cm-more { max-width: 980px; margin: 40px auto 0; }
  .cm-more-eyebrow {
    display: block; font-size: 0.78rem; letter-spacing: 0.08em;
    text-transform: uppercase; font-weight: 700; margin-bottom: 8px;
    color: var(--muted, #5f6f7a);
  }
  .cm-more-card {
    display: block; border: 1px solid var(--line, #e3e8ea); border-radius: 12px;
    padding: 16px 18px; text-decoration: none; color: inherit;
    background: var(--card, #fff); transition: border-color 0.12s;
  }
  .cm-more-card:hover { border-color: var(--accent, #14655f); }
  .cm-more-card strong { display: block; margin-bottom: 4px; }
  .cm-more-card span { font-size: 0.92rem; color: var(--muted, #5f6f7a); }
</style>
